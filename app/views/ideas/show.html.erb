
<div id="<%= dom_id @idea %>" class="d-flex gap-3 py-3">
  <div class="d-flex flex-column gap-2 w-100">
    <h1><%= @idea.name %></h1>
    <p><%= @idea.description %></p>
    <small class="opacity-50 text-nowrap">Last updated <%= time_ago_in_words @idea.updated_at %></small>
  </div>
  <%= image_tag(@idea.picture_url, width: 150, height: 150, class: "img-thumbnail flex-shrink-0") if @idea.picture? %>
</div>

<div class="d-flex gap-3 py-3">
  <%= link_to "投稿を修正する", edit_idea_path(@idea), class: "btn btn-primary" %>
  <%= link_to "idea一覧に戻る", ideas_path, class: "btn btn-outline-secondary" %>
  <%= button_to "この投稿を削除する", @idea, method: :delete, class: "btn btn-danger", form: { data: { turbo_confirm: "削除しますか？" } } %>
</div>

<h2>コメント</h2>
<% if @comments.any? %>
  <% @comments.each do |comment| %>
    <div>
      <p><strong><%= comment.user_name %></strong></p>
      <p><%= comment.body %></p>
      <%= button_to "このコメントを削除する", idea_comment_path(@idea, comment), method: :delete, class: "btn btn-danger", form: { data: { turbo_confirm: "Are you sure?" } } %>
    </div>
  <% end %>
<% else %>
  <p>No comments found.</p>
<% end %>

<h2>コメント投稿欄</h2>
<%= render partial: "comments/form", locals: { idea: @idea } %>